# Image tags for local builds. For EC2/kind production builds, change this to
# something like ghcr.io/<github-owner>/clo835-project and a commit SHA tag.
IMAGE_REPOSITORY_PREFIX=clo835-project
IMAGE_TAG=local

# Browser/static client
CLIENT_HOST_PORT=8080

# Main server
MAIN_SERVER_HOST_PORT=3000

# Task orchestrator
TASK_ORCHESTRATOR_HOST_PORT=3001
