apiVersion: v1
kind: Service
metadata:
  name: main-server-service
  namespace: orch-109920256
spec:
  selector:
    app: main-server
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  type: ClusterIP
