apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  namespace: orch-109920256
spec:
  selector:
    app: nginx-web
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080
  type: NodePort
