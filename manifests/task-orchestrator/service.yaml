apiVersion: v1
kind: Service
metadata:
  name: task-orchestrator-service
  namespace: orch-109920256
spec:
  selector:
    app: task-orchestrator
  ports:
    - protocol: TCP
      port: 3001
      targetPort: 3001
  type: ClusterIP
